(function () {
    'use strict';
    window.MYMATE_CONFIG = Object.freeze({
        app: 'GridMap',
        version: '2.5.87',
        label: 'GridMap v2.5.87',
        assetVersion: 'v2-5-87-gridmap',
        cacheName: 'gridmap-public-v2-5-87',
        dataSchemaVersion: 51,
        releaseInfoUrl: 'version.json',
        releaseDate: '2026-06-16',
        releaseNotes: [
            'Fixed the Reset panel markup so it is a normal Settings section again, not nested inside Developer.',
            'Kept Reset button behaviour unchanged.',
            'Kept the padded maskable launch icons so the loading icon is not clipped.',
            'Updated manifest and service-worker cache versions so the PWA refreshes cleanly.'
        ]});
})();
